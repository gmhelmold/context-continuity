{
  "name": "context-continuity-workspace",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.17.1"
  },
  "scripts": {
    "check:core": "tsc --project tsconfig.core.json",
    "test:core": "node --experimental-strip-types --test tests/core/*.test.mjs",
    "check:storage": "tsc --project tsconfig.storage.json",
    "test:storage": "node --experimental-strip-types --test tests/storage/*.test.mjs",
    "build:locks": "node scripts/build-locks.mjs",
    "test:coordination": "node --experimental-strip-types --test tests/coordination/*.test.mjs"
  },
  "devDependencies": {
    "@types/node": "22.17.1",
    "typescript": "5.9.3"
  }
}
